# Contoh konfigurasi - salin ke .env lalu isi nilainya.
# JANGAN commit berkas .env yang berisi kredensial asli.

# DSN PostgreSQL. Untuk Supabase/Neon, salin "Connection string" (mode pooler/transaction).
# Wajib sslmode=require untuk koneksi hosted.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:6543/postgres?sslmode=require

# Opsional: DSN khusus migrasi. Gunakan direct/session pooler (mis. port 5432)
# karena golang-migrate memakai advisory lock level sesi.
# Bila kosong, aplikasi memakai DATABASE_URL dan mencoba menyesuaikan port pooler 6543 -> 5432.
MIGRATION_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require

# Opsional: DB terpisah untuk integration test. Jangan arahkan ke DB produksi.
TEST_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/koperasi_test?sslmode=require

# Secret untuk menandatangani cookie sesi (FR-SEC-02). Gunakan string acak panjang.
SESSION_SECRET=ganti-dengan-string-acak-minimal-32-karakter

# true saat aplikasi dilayani melalui HTTPS (wajib untuk produksi).
SESSION_SECURE=false

# Port HTTP server (opsional, default 8080).
PORT=8080
